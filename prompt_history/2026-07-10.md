# 2026-07-10

## Skill to build physique-guru Agents
Create a skill that helps or updates a Claude Agent unique to this project. Agents to assist the physique-guru agent.

The physique-guru agent is the primary agent that coordinates the other subagents located on the `.claude/agents` folder.

Analyze these agents to determine how additional subagents should be built or updated. Changes to subagents may need to update the physique-guru agent for consistency.

The already built build-agent skill will always be called and if not this skill calls it. So this skill should add value in addition to that skill not make a redundant skill.

This skill also calls the plan skill.

/plan

## Build an MVP product
Build an MVP product

Here is the goal. Operator asks a question and the chat responds.
- Chat may ask the operator a question
- Chat uses the physique-guru agent in `.claude/agents`
- Use the Claude SDK

Interview me for alingment

Review your answers
• What runtime/language should the MVP app be built in?
→ TypeScript/Node
• What should the operator's interface be for this MVP?
→ Terminal chat loop
 How should the app invoke the physique guru agent via the SDK?
→ Let physique-guru orchestrate as-is
• Should the MVP support multi-turn conversation, or one question in / one answer out?→  Single-turn only


## Coaching Program SKill
I will be working with the Physique Guru agent in the `.claude/agents` folder. We will iterate together over several sessions to create, build, and optimize a physique coaching program.

First let’s build a skill that understands our intent. The skill should also understand that this is an iterative process. At the end of every session, only with direct permission, capture the entire conversation log + a distilled version of it best suited for future ai chats like this. Make sure these files are  timestamped. And future ai chats like this know that the distilled version of past chats exist and can further investigate the full conversation history  if needed.

Create the coaching plan as a markdown file or a set of markdown files if that is a better design organized in a subfolder BUT only with direct permission. Not every session ends with an updated coaching plan.

Organize into the coaching_program folder.

Before we start, interview me to align on a plan. Including reviewing the design of this prompt itself for this project.

Review your answers
* ﻿﻿How should ending a session and offering to save logs be triggered?→ Explicit command only
* ﻿﻿What should the 'full conversation log' actually contain?
- ﻿﻿Reconstructed clean transcript
* ﻿﻿How should the coaching plan itself be structured in coaching_program/?→ Start with a single file and as the coaching program develops overtime understand it will make sense to split it up.
* ﻿﻿What timestamp granularity for session log filenames?→ Date + rev e.g. 2026-07-19_007

Great work

* Let’s review this line in the Skill

“””
Only split it into
  `plan/overview.md` + `plan/training.md` + `plan/nutrition.md` once it has
  genuinely outgrown one file
“””

The split is not yet decided. Still to be figured out. Let’s not make this what we do for sure.

* Coaching plan also needs to be dated w/ a rev

Interview me to align on a plan.


Review your answers
• When the coaching plan is updated, → Overwrite in place, stamp only
what happens to the previous version?
• How should the plan's revision number be assigned?
→ Tied to the session sequence number